name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.1.0)'
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Resolve tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "name=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "name=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Validate skill structure
        run: |
          test -f skills/joomla/SKILL.md || { echo "::error::skills/joomla/SKILL.md missing"; exit 1; }
          head -1 skills/joomla/SKILL.md | grep -q '^---$' || { echo "::error::SKILL.md missing YAML frontmatter"; exit 1; }

      - name: Build skill zip
        id: zip
        env:
          TAG: ${{ steps.tag.outputs.name }}
        run: |
          out="joomla-skill-${TAG}.zip"
          (cd skills && zip -r "../${out}" joomla -x "*.DS_Store" "*/.DS_Store")
          ls -lh "${out}"
          echo "file=${out}" >> "$GITHUB_OUTPUT"

      - name: Build universal package zip
        id: universal
        env:
          TAG: ${{ steps.tag.outputs.name }}
        run: |
          bash scripts/build-universal.sh
          out="joomla-skill-universal-${TAG}.zip"
          (cd dist && mv universal "joomla-skill-universal-${TAG}" \
            && zip -r "../${out}" "joomla-skill-universal-${TAG}" \
                 -x "*.DS_Store" "*/.DS_Store")
          ls -lh "${out}"
          echo "file=${out}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          name: ${{ steps.tag.outputs.name }}
          generate_release_notes: true
          files: |
            ${{ steps.zip.outputs.file }}
            ${{ steps.universal.outputs.file }}
          body: |
            **Install in Claude Code:**
            ```
            /plugin marketplace add Joomla-Bible-Study/claude-skill-joomla
            /plugin install joomla@joomla-bible-study
            /reload-plugins
            ```

            **Install in Claude.ai (consumer app):**
            Download `${{ steps.zip.outputs.file }}` below, then upload at https://claude.ai/settings/capabilities → Skills → Create skill.

            **Other AI coding tools (Cursor, GitHub Copilot, Windsurf, Cline, Aider, Codex / AGENTS.md):**
            Download `${{ steps.universal.outputs.file }}` and follow the install table in its bundled `README.md`. One file per tool, all sharing the same `references/` directory.

            See [README](https://github.com/Joomla-Bible-Study/claude-skill-joomla#readme) for full instructions.
